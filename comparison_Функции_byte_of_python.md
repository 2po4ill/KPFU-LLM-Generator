# Функции
**Book**: A Byte of Python
**Level**: beginner
**Generation Time**: 248.6s
**Word Count**: 2364
**Confidence**: 100%

---

# ЛЕКЦИЯ ПО ДИСЦИПЛИНЕ ""

**Направление подготовки:** Информатика
**Уровень образования:** bachelor
**Кафедра:** Не указана

---

**Введение**

Функции являются одним из наиболее важных и многогранных инструментов программирования. Они представляют собой многократно используемые фрагменты программы, которые могут быть вызваны повторно в любом месте кода с помощью их имени. В этом разделе мы рассмотрим основные аспекты функций, включая их определение, параметры, локальные переменные и значения аргументов по умолчанию.

**Описание функций как многократно используемых фрагментов программы**

Функции — это блоки кода, которые могут быть вызваны повторно в любом месте программы. Они позволяют разделить программу на логически связанные части, каждая из которых выполняет конкретную задачу. Функции можно сравнить с функциями в математике: они принимают входные данные (аргументы), производят некоторую работу и возвращают результат.

**Объяснение того, что функции позволяют давать имя определённому блоку команд для последующего вызова**

Функции позволяют давать имя определённому блоку команд, который можно вызвать повторно с помощью этого имени. Это позволяет избежать дублирования кода и упрощает поддержку программы. Например, если вы хотите вывести приветствие на экран несколько раз, вместо того чтобы написать этот же блок кода несколько раз, вы можете определить функцию `sayHello()` и вызвать ее повторно.

**Краткий обзор важности функций в программировании**

Функции являются фундаментальным элементом программирования. Они позволяют разделить программу на логически связанные части, каждая из которых выполняет конкретную задачу. Функции также позволяют избежать дублирования кода и упрощают поддержку программы.

**Определение функций**

Функция определяется с помощью зарезервированного слова `def`. После этого слова указывается имя функции, за которым следует пара скобок, в которых можно указать имена некоторых переменных, и заключительное двоеточие в конце строки. Далее следует блок команд, составляющих функцию.

Пример:
```python
# Пример со страницы 67
def sayHello():
    print('Привет, Мир!')
```
**Параметры функций**

Функции могут принимать параметры, т.е. некоторые значения, которые передаются функции для того, чтобы она что-либо сделала с ними. Параметры указываются в скобках при объявлении функции и разделяются запятыми.

Пример:
```python
# Пример со страницы 68
def printMax(a, b):
    if a > b:
        print(a, 'максимально')
    elif a == b:
        print(a, 'равно', b)
    else:
        print(b, 'максимально')
```
**Локальные переменные**

При объявлении переменных внутри определения функции они не связаны с другими переменными с таким же именем за пределами функции. Имена переменных являются локальными в функции.

Пример:
```python
# Пример со страницы 69
x = 50
def func(x):
    print('x равно', x)
    x = 2
    print('Замена локального x на', x)
```
**Зарезервированное слово «global»**

Чтобы присвоить некоторое значение переменной, определённой на высшем уровне программы (т.е. не в какой-либо

**Основные концепции**

В этом разделе мы рассмотрим основные концепции функций в Python. Функции - это многократно используемые фрагменты программы, которые позволяют дать имя определённому блоку команд и запускать этот блок по указанному имени в любом месте программы.

**10.1 Параметры функций**

Функции могут принимать параметры, т.е. некоторые значения, передаваемые функции для того, чтобы она что-либо сделала с ними. Эти параметры похожи на переменные, за исключением того, что значение этих переменных указывается при вызове функции, и во время работы функции им уже присвоены их значения.

Параметры указываются в скобках при объявлении функции и разделяются запятыми. Аналогично мы передаём значения, когда вызываем функцию. Обратите внимание на терминологию: имена, указанные в объявлении функции, называются параметрами, а то, что передаются в функцию при её вызове, – аргументами.

**Пример:** (сохранить как func_param.py)
```python
def printMax(a, b):
    if a > b:
        print(a, 'максимально')
    elif a == b:
        print(a, 'равно', b)
    else:
        print(b, 'максимально')
printMax(3, 4) # прямая передача значений
x = 5
y = 7
printMax(x, y) # передача переменных в качестве аргументов
```
Вывод:
$ python func_param.py
4 максимально
7 максимально

Как это работает:

Здесь мы определили функцию с именем printMax, которая использует два параметра с именами a и b. Мы находим наибольшее число с помощью простого оператора if..else и выводим его.

При первом вызове функции printMax мы напрямую передаём числа в качестве аргументов. Во втором случае мы вызываем функцию с переменными в качестве аргументов. printMax(x, y) назначает значение аргумента x параметру a, а значение аргумента y – параметру b. В обоих случаях функция printMax работает одинаково.

**10.2 Локальные переменные**

При объявлении переменных внутри определения функции они не связаны с другими переменными с таким же именем за пределами функции – т.е. имена переменных являются локальными в функции. Это называется областью видимости переменной.

Область видимости всех переменных ограничена блоком, в котором они объявлены, начиная со строки объявления имени.

**Пример:** (сохранить как func_local.py)
```python
x = 50
def func(x):
    print('x равно', x)
    x = 2
    print('Замена локального x на', x)
func(x)
print('x по прежнему', x)
```
Вывод:
$ python func_local.py
x равно 50
Замена локального x на 2
x по прежнему 50

Как это работает:

При первом выводе значения, присвоенного имени x, в первой строке функции Python использует значение параметра, объявленного в основном блоке, выше определения функции.

Далее мы назначаем x значение 2. Имя x локально для нашей функции. По этому у нас замена значения x в функции не затронет значение x, объявленное в основном блоке.

Последний вызов функции print выводит значение x, указанное в основном блоке, подтверждая таким образом, что оно не изменилось при локальном присваивании значения внутри вызванной функции.

**10.3 Зарезервированное слово «global»**

Чтобы присвоить некоторое значение переменной, определённой на высшем уровне программы (т.е. не в какой-либо области видимости, как то функции или классов), необходимо указать Python, что она является глобальной (global). Сделаем это с помощью зарезервированного слова global. Без применения зарезервированного слова global невозможно присвоить значение переменной, определённой за пределами функции.

Можно использовать уже существующие значения переменных, определённые за пределами функции (при условии, что внутри функции не было объявлено переменная с таким же именем). Однако, это не приветствуется, и его следует избегать, поскольку человеку, читающему текст программы, будет непонятно, где находится объявление переменной. Использование зарезервированного слова global достаточно ясно показывает, что переменная объявлена в самом внешнем блоке.

**Пример:** (сохранить как func_global.py)
```python
x = 50
def func():
    global x
    print('x равно', x)
    x = 2
    print('Заменяем глобальное значение x на', x)
func()
print('Значение x составляет', x)
```
Вывод:
$ python func_global.py
x равно 50
Заменяем глобальное значение x на 2
Значение x составляет 2

Как это работает:

Зарезервированное слово global используется для того, чтобы объявить, что x – это глобальная переменная, а значит, когда мы присваиваем значение имени x внутри функции, это изменение отразится на значении переменной x в основном блоке программы.

Используя одно зарезервированное слово global, можно объявить сразу несколько переменных: global x, y, z.

**10.4 Зарезервированное слово «nonlocal»**

Мы увидели, как получать доступ к переменным в локальной и глобальной области видимости. Есть ещё одна область видимости, называемая «нелокальной» (nonlocal) областью видимости, которая представляет собой нечто среднее между первыми двумя. Нелокальные области видимости встречаются, когда вы определяете функцию внутри функций.

Поскольку в Python всё является выполнимым кодом, вы можете определять функции где угодно.

**Пример:** (сохранить как func_nonlocal.py)
```python
def func_outer():
    x = 2
    print('x равно', x)
    def func_inner():
        nonlocal x
        x = 5
    func_inner()
    print('Локальное x сменилось на', x)
func_outer()
```
Вывод:
$ python func_nonlocal.py
x равно 2
Локальное x сменилось на 5

Как это работает:

Когда мы находимся внутри func_inner, переменная x, определённая в первой строке func_outer находится в нелокальной области видимости (определение переменной не включает в себя блок func_inner). Мы объявляем, что хотим использовать именно эту переменную x, следующим образом: nonlocal x.

Попробуйте заменить «nonlocal x» на «global x», а затем удалите это зарезервированное слово, и пронаблюдайте за разницей между этими двумя случаями.

**10.5 Значения аргументов по умолчанию**

Зачастую часть параметров функций могут быть необязательными, и для них будут использоваться некоторые заданные значения по умолчанию, если пользователь не укажет свои собственные. Это можно достичь с помощью значений аргументов по умолчанию. Их можно указать, добавив к имени параметра в определении функции оператор присваивания (=) следующим значением.

Обратите внимание, что значение по умолчанию должно быть константой. Или точнее говоря, оно должно быть неизменным (1) – это объясняется подробно в последующих главах. А как напомнить это?

В заключении мы рассмотрели основные концепции функций в Python: параметры функций, локальные переменные, зарезервированное слово «global», нелокальные области видимости и значения аргументов по умолчанию. Эти концепции являются важными инструментами для написания эффективной и читаемой программы на языке Python.

**Примеры кода**

В предыдущих разделах мы рассмотрели основные аспекты функций в Python, включая их определение, параметры и область видимости. В этом разделе мы продолжим изучать примеры кода, демонстрирующие работу функций.

**10.1 Параметры функций**

Функции могут принимать параметры, т. е. некоторые значения, передаваемые функции для того, чтобы она что-либо сделала с ними. Эти параметры похожи на переменные, за исключением того, что значение этих переменных указывается при вызове функции, и во время работы функции им присваиваются значения.

Параметры указываются в скобках при объявлении функции и разделяются запятыми. Аналогично мы передаём значения, когда вызываем функцию. Обратите внимание на терминологию: имена, указанные в объявлении функции, называются параметрами, а те значения, которые передаются при вызове функции, — аргументами.

# Пример со страницы 68
def printMax(a, b):
if a > b:
print(a, 'максимально')
elif a == b:
print(a, 'равно', b)
else:
print(b, 'максимально')
printMax(3, 4) # прямая передача значений
x = 5
y = 7
printMax(x, y) # передача переменных в качестве аргументов

Вывод:
$ python func_param.py
4 максимально
7 максимально

Как работает:
Здесь мы определили функцию с именем printMax, которая использует два параметра с именами a и b. Мы находим наибольшее число с помощью простого оператора if..else и выводим это число.

При первом вызове функции printMax мы напрямую передаём числа в качестве аргументов. Во втором случае мы вызываем функцию с переменными в качестве аргументов. printMax(x, y) назначает значение аргумента x параметру a, а значение аргумента y – параметру b. В обоих случаях функция printMax работает одинаково.

**10.2 Локальные переменные**

При объявлении переменных внутри определения функции они не связаны с другими переменными с таким же именем за пределами функции – т. е. имена переменных являются локальными в функции. Это называется областью видимости переменной.

# Пример со страницы 69
x = 50
def func(x):
print('x равен', x)
x = 2
print('Замена локального x на', x)
func(x)
print('x по прежнему', x)

Вывод:
$ python func_local.py
x равен 50
Замена локального x на 2
x по прежнему 50

Как работает:
При первом выводе значения, присвоенного имени x, в первой строке функции Python использует значение параметра, объявленного в основном блоке, выше определения функции.

Далее мы назначаем x значение 2. Имя x локально для нашей функции. По этому у нас замена значения x в функции не отразится на значении x в основном блоке программы.

Последний вызов функции print выводит значение x, указанное в основном блоке, подтверждая таким образом, что оно не изменилось при локальном присваивании значения в вызванной функции.

**10.3 Зарезервированное слово «global»**

Чтобы присвоить некоторое значение переменной, определённой на высшем уровне программы (т. е. не в какой-либо области видимости, как то функции или классов), необходимо указать Python, что это имя локально, а глобально (global). Сделаем это с помощью зарезервированного слова global.

# Пример со страницы 70
x = 50
def func():
global x
print('x равно', x)
x = 2
print('Заменяем глобальное значение x на', x)
func()
print('Значение x составляет', x)

Вывод:
$ python func_global.py
x равно 50
Заменяем глобальное значение x на 2
Значение x составляет 2

Как работает:
Зарезервированное слово global используется для того, чтобы объявить, что x – это глобальная переменная. А именно, когда мы присваиваем значение имени x в функции, это изменение отражается на значении переменной x в основном блоке программы.

Используя одно зарезервированное слово global, можно объявить сразу несколько переменных: global x, y, z.

**10.4 Зарезервированное слово «nonlocal»**

Мы увидели, как получать доступ к переменным в локальной и глобальной области видимости. Есть ещё один тип области видимости, называемый «нелокальной» (nonlocal) областью видимости, который представляет собой нечто среднее между первыми двумя.

# Пример со страницы 71
def func_outer():
x = 2
print('x равно', x)
def func_inner():
nonlocal x
x = 5
func_inner()
print('Локальное x

**Практические советы**

В предыдущих разделах мы обсуждали основные аспекты функций в Python, включая их определение, параметры и область видимости переменных. В этом разделе мы предоставим практические советы по написанию эффективных функций с учетом повторного использования кода, важности тестирования и отладки функций, а также оптимизации работы функций для улучшения производительности программы.

**Рекомендации по написанию эффективных функций**

1. **Упрощайте функции**: Функции должны быть простыми и понятными. Упрощайте их до минимума, чтобы они выполняли одну конкретную задачу.
2. **Используйте параметры**: Параметры функций позволяют передавать входные данные в функцию, что делает ее более гибкой и повторно используемой.
3. **Употребляйте значения по умолчанию**: Значения по умолчанию для параметров функции могут быть полезны, если пользователь не хочет указывать конкретное значение.
4. **Используйте локальные переменные**: Локальные переменные позволяют избежать конфликтов имен с глобальными переменными.
5. **Тестируйте и отлаживайте функции**: Тестируйте функции на различных входных данных, чтобы убедиться в их правильности.

**Обсуждение важности тестирования и отладки функций**

Тестирование и отладка функций являются критически важными шагами в разработке программного обеспечения. Они позволяют выявить ошибки и баги в функциях, что может привести к серьезным последствиям, если они не будут исправлены.

**Советы по оптимизации работы функций**

1. **Используйте эффективные алгоритмы**: Выберите алгоритмы, которые имеют низкую сложность и могут выполняться быстро.
2. **Упрощайте циклы**: Упрощайте циклы, чтобы они выполнялись быстрее.
3. **Используйте кэширование**: Кэширование может помочь уменьшить количество вычислений и улучшить производительность функций.

**Примеры кода**

# Пример со страницы 67
def sayHello():
    print('Привет, Мир!')
sayHello()
sayHello()

# Пример со страницы 68
def printMax(a, b):
    if a > b:
        print(a, 'максимально')
    elif a == b:
        print(a, 'равно', b)
    else:
        print(b, 'м

**Заключение**

В этом разделе мы проанализируем все изученные концепции функций в программировании, обсудим перспективы использования функций в дальнейшем развитии навыков программирования и предоставим рекомендации для дальнейшего изучения темы.

Функции - это многократно используемые фрагменты программы, которые позволяют дать имя определённому блоку команд с целью запуска этого блока по указанному имени в любом месте программы. Это называется вызовом функции. Мы уже использовали многие встроенные функции, такие как len и range.

Функция определяется при помощи зарезервированного слова def. После этого слова указывается имя функции, за которым следует пара скобок, в которых можно указать имена некоторых переменных, и заключительное двоеточие в конце строки. Далее следует блок команд, составляющих функцию.

Например, мы можем определить функцию с именем sayHello следующим образом:

# Пример со страницы 59
def sayHello():
    print('Привет, Мир!') # блок, принадлежащий функции

Мы можем вызывать одну и ту же функцию много раз, не обязательно переписывая один и тот же код снова и снова.

Функции могут принимать параметры - входные данные, которые мы можем передать функции для получения соответствующего результата. Параметры указываются в скобках при объявлении функции и разделяются запятыми. Аналогично, мы передаём значения, когда вызываем функцию.

Например, мы можем определить функцию с именем printMax следующим образом:

# Пример со страницы 60
def printMax(a, b):
   

---

**Дата составления:** 25.02.2026
