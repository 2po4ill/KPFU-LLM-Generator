# Функции
**Book**: ООП на Python
**Level**: advanced
**Generation Time**: 349.8s
**Word Count**: 2467
**Confidence**: 100%

---

# ЛЕКЦИЯ ПО ДИСЦИПЛИНЕ ""

**Направление подготовки:** Информатика
**Уровень образования:** bachelor
**Кафедра:** Не указана

---

**Введение**

Раздел "Функции" является важнейшим аспектом программирования, и в этом разделе мы будем изучать основные принципы и концепции функций. Функции представляют собой обособленный участок кода, который можно вызывать по имени, и они играют решающую роль в организации и повторном использовании кода.

**Описание темы и цели лекции**

Целью этого раздела является предоставление основной информации о функциях в программировании. Мы будем изучать определение функций, их назначение, а также различные способы использования функций в программе. В частности, мы рассмотрим следующие ключевые точки:

* Определение и использование функций
* Параметры и возвращаемые значения функций
* Функции как обособленные блоки кода
* Преимущества использования функций

**Объяснение важности функций в программировании**

Функции являются фундаментальным понятием в программировании, и они играют решающую роль в организации и повторном использовании кода. Функции позволяют разделить программу на обособленные блоки кода, каждый из которых выполняет конкретную задачу. Это делает программы более организованными, читаемыми и поддерживаемыми.

Функции также могут быть вызваны повторно в разных местах программы, что позволяет избежать дублирования кода. Кроме того, функции могут принимать аргументы и возвращать значения, что делает их еще более полезными.

**Краткий обзор содержания лекции**

В этом разделе мы будем изучать следующие ключевые точки:

* Определение функций и их назначение
* Параметры и возвращаемые значения функций
* Функции как обособленные блоки кода
* Преимущества использования функций

Мы также рассмотрим примеры кода, которые демонстрируют использование функций в различных сценариях.

**Определение функций и их назначение**

Функция представляет собой обособленный участок кода, который можно вызывать по имени. Функции могут принимать аргументы и возвращать значения, что делает их еще более полезными.

# Пример со страницы 8
def count_food():
    a = int(input())
    b = int(input())
    print("Всего", a+b, "шт.")

Функция `count_food()` принимает два аргумента: `a` и `b`, которые являются целыми числами. Функция возвращает строку с суммой двух аргументов.

**Параметры и возвращаемые значения функций**

Функции могут принимать аргументы, которые передаются при вызове функции. Аргументы могут быть любого типа данных: целое число, строка, список и т. д.

# Пример со страницы 14
def cylinder():
    r = float(input())
    h = float(input())
    side = 2 * 3.14 * r * h
    circle = 3.14 * r**2
    full = side + 2 * circle
    return full

Функция `cylinder()` принимает два аргумента: `r` и `h`, которые являются вещественными числами. Функция возвращает значение, которое представляет собой полную площадь цилиндра.

**Функции как обособленные блоки кода**

Функции представляют собой обособленный участок кода, который можно вызывать по имени. Это делает

**Основные концепции**

В этом разделе мы рассмотрим основные концепции программирования, которые являются фундаментальными для понимания языка Python. Мы начнем с описания функций как обособленных участков кода, которые можно вызывать по имени.

Функции в программировании представляют собой обособленный участок кода, который можно вызывать, обратившись к нему по имени, которым он был назван. При вызове происходит выполнение команд тела функции. Функции можно сравнить с небольшими программками, которые сами по себе, то есть автономно, не исполняются, а встраиваются в обычную программу.

Нередко их так и называют – подпрограммы. Других ключевых отличий функций от программ нет. Функции также при необходимости могут получать и возвращать данные. Только обычно они их получают не с ввода (клавиатуры, файла и др.), а из вызывающей программы. Сюда же они возвращают результат своей работы.

Существует множество встроенных в язык программирования функций. С некоторыми такими в Python мы уже сталкивались. Это print(), input(), int(), float(), str(), type(). Код их тела нам не виден, он где-то "спрятан внутри языка". Нам же предоставляется только интерфейс – имя функции.

С другой стороны, программист всегда может определять свои функции. Их называют пользовательскими. В языке программирования Python функции определяются с помощью оператора def. Рассмотрим код:

def count_food():
a = int(input())
b = int(input())
print("Всего", a+b, "шт.")

Это пример определения функции. Как и другие сложные инструкции вроде условного оператора и циклов функция состоит из заголовка и тела. Заголовок оканчивается двоеточием и переходом на новую строку. Тело имеет отступ.

Ключевое слово def сообщает интерпретатору, что перед ним определение функции. За def следует имя функции. Оно может быть любым, так же, как и всякий идентификатор. В программировании весьма желательно давать всему осмысленные имена. Так в данном случае функция названа "посчитать_еду" в переводе на русский.

После имени функции ставятся скобки. В приведенном примере они пустые. Это значит, что функция не принимает никакие данные из вызывающей ее программы. Однако она могла бы их принимать, и тогда в скобках были бы указаны параметры этой функции.

После двоеточия следует тело, содержащее инструкции, которые выполняются при вызове функции. Следует различать определение функции и ее вызов. В программном коде они не рядом и не вместе. Можно определить функцию, но ни разу ее не вызвать. Нельзя вызвать функцию, которая не была определена.

Определив функцию, но ни разу не вызвав ее, вы никогда не выполните ее тела. Определение функции должно предшествовать ее вызовам. Это связано с тем, что интерпретатор читает код строка за строкой и о том, что находится ниже по течению, ему еще неизвестно.

Функции могут передавать какие-либо данные из своих тел в основную ветку программы. Говорят, что функция возвращает значение. В большинстве языков программирования, в том числе Python, выход из функции и передача данных в то место, откуда она была вызвана, выполняется оператором return.

Если интерпретатор Питона, выполняя тело функции, встречает return, то он "забирает" значение, указанное после этой команды, и "уходит" из функции. def cylinder():
r = float(input())
h = float(input())
# площадь боковой поверхности цилиндра:
side = 2 * 3.14 * r * h
# площадь одного основания цилиндра:
circle = 3.14 * r**2
# полная площадь цилиндра:
full = side + 2 * circle
return full

В Питоне позволительно возвращать из функции несколько объектов, перечислив их через запятую после команды return:

def cylinder():
r = float(input())
h = float(input())
side = 2 * 3.14 * r * h
circle = 3.14 * r**2
full = side + 2 * circle
return side, full

Фокус здесь в том, что перечисление значений через запятую (например, 10, 15, 19) создает объект типа tuple. На русский переводится как "кортеж". Когда же кортеж присваивается сразу нескольким переменным, то происходит сопоставление его элементов соответствующим в очереди переменным. Это называется распаковкой.

Функции могут не только возвращать данные, но также принимать их, что реализуется с помощью так называемых параметров, которые указываются в скобках в заголовке функции. Количество параметров может быть любым.

Параметры представляют собой локальные переменные, которым присваиваются значения в момент вызова функции. Конкретные значения, которые передаются в функцию при ее вызове, называются аргументами. Когда функция вызывается, то ей передаются фактические значения аргументов.

Т.е. переменным-параметрам присваиваются переданные в функцию значения. Следовательно, изменение значений в теле функции никак не скажется на значениях фактических переменных. Они останутся прежними. В Python такое поведение характерно для неизменяемых типов данных, к которым относятся, например, числа и строки.

Существуют изменяемые типы данных. Для Питона, это, например, списки и словари. В этом случае данные передаются по ссылке. В функцию передается ссылка на них, а не сами данные. И эта ссылка связывается с локальной переменной.

Изменения таких данных через локальную переменную обнаруживаются при обращении к ним через глобальную. Это есть следствие того, что несколько переменных ссылаются на одни и те же данные, на одну и ту же область памяти. Необходимость передачи по ссылке связана в первую очередь с экономией памяти.

Сложные типы данных, по сути представляющие собой структуры данных, обычно копировать не целесообразно. Однако, если надо, всегда можно сделать это принудительно.

В следующем разделе мы рассмотрим модули и пакеты в языке Python.

**Модули**

Встроенные в язык программирования функции доступны сразу. Чтобы их вызвать, не нужно выполнять никаких дополнительных действий. Однако за время существования языка на нем было написано столько функций и классов, которые оказались востребованными множеством программистов и в разных областях, что включить весь этот объем кода в сам язык если возможно, то нецелесообразно.

Чтобы разрешить проблему доступа к дополнительным возможностям языка, в программировании стало общепринятой практикой использовать так называемые модули, пакеты и библиотеки. Каждый модуль содержит коллекцию функций и классов, предназначенных для решения задач из определенной области.

Так в модуле math языка Python содержатся математические функции, модуль random позволяет генерировать псевдослучайные числа, в модуле datetime содержатся классы для работы с датами и временем, модуль sys предоставляет доступ к системным переменным и т. д.

Количество модулей для языка Python огромно, что связано с популярностью языка. Часть модулей собрана в так называемую стандартную библиотеку. Стандартная она потому, что поставляется вместе с установочным пакетом. Однако существуют сторонние библиотеки.

Они скачиваются и устанавливаются отдельно. Для доступа к функционалу модуля, его нужно импортировать в программу. После импорта интерпретатор "знает" о существовании дополнительных классов и функций и позволяет ими пользоваться.

В Питоне импорт осуществляется командой import. При этом существует несколько способов импорта. Рассмотрим работу с модулем на примере math:

import math

Мы можем использовать функцию sin() из модуля math, вызывая ее по имени:

sin = math.sin()

Или мы можем импортировать только конкретную функцию:

from math import sin

В этом случае мы не можем использовать

**Примеры кода**

В этом разделе мы рассмотрим различные примеры кода, которые демонстрируют использование функций в языке Python.

**Пример 1. Определение и вызов функции**

На странице [13] приведен пример определения функции `count_food()`:
```python
def count_food():
    a = int(input())
    b = int(input())
    print("Всего", a+b, "шт.")
```
Функция принимает два целых числа от пользователя и возвращает их сумму.

Чтобы вызвать эту функцию, мы можем использовать следующий код:
```python
print("Сколько бананов и ананасов для обезьян?")
count_food()
print("Сколько жуков и червей для ежей?")
count_food()
print("Сколько рыб и моллюсков для выдр?")
count_food()
```
**Пример 2. Возврат значения из функции**

На странице [14] приведен пример использования функции `cylinder()`:
```python
def cylinder():
    r = float(input())
    h = float(input())
    side = 2 * 3.14 * r * h
    circle = 3.14 * r**2
    full = side + 2 * circle
    return full
```
Функция принимает два вещественных числа от пользователя и возвращает полную площадь цилиндра.

Чтобы вызвать эту функцию, мы можем использовать следующий код:
```python
square = cylinder()
print(square)
```
**Пример 3. Возврат нескольких значений из функции**

На странице [14] приведен пример использования функции `cylinder()` с возвратом двух значений:
```python
def cylinder():
    r = float(input())
    h = float(input())
    side = 2 * 3.14 * r * h
    circle = 3.14 * r**2
    full = side + 2 * circle
    return side, full
```
Функция принимает два вещественных числа от пользователя и возвращает площадь боковой поверхности цилиндра и полную площадь цилиндра.

Чтобы вызвать эту функцию, мы можем использовать следующий код:
```python
sCyl, fCyl = cylinder()
print("Площадь боковой поверхности %.2f" % sCyl)
print("Полная площадь %.2f" % fCyl)
```
**Пример 4. Распаковка кортежа**

На странице [14] приведен пример использования функции `cylinder()` с возвратом кортежа:
```python
def cylinder():
    r = float(input())
    h = float(input())
    side = 2 * 3.14 * r * h
    circle = 3.14 * r**2
    full = side + 2 * circle
    return side, full
```
Функция принимает два вещественных числа от пользователя и возвращает кортеж с площадью боковой поверхности цилиндра и полной площадью цилиндра.

Чтобы вызвать эту функцию, мы можем использовать следующий код:
```python
sCyl, fCyl = cylinder()
print("Площадь боковой поверхности %.2f" % sCyl)
print("Полная площадь %.2f" % fCyl)
```
**Пример 5. Параметры функции**

На странице [15] приведен пример использования функции `cylinder()` с параметрами:
```python
def cylinder(r, h):
    side = 2 * 3.14 * r * h
    circle = 3.14 * r**2
    full = side + 2 * circle
    return full
```
Функция принимает два вещественных числа `r` и `h` от пользователя и возвращает полную площадь цилиндра.

Чтобы вызвать эту функцию, мы можем использовать следующий код:
```python
square = cylinder(5.0, 3.0)
print(square)
```
**Пример 6. Импорт модулей**

На странице [18] приведен пример использования импорта модуля `math`:
```python
import math
```
Модуль `math` содержит функции для математических операций.

Чтобы использовать функцию из модуля `math`, мы можем использовать следующий код:
```python
print(math.pi)
```
**Пример 7. Наследование**

На странице [44] приведен пример использования наследования классов:
```python
class Animal:
    def __init__(self, name):
        self.name = name

class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)
        self.breed = breed
```
Класс `Dog` наследует свойства и методы класса `Animal`.

Чтобы использовать этот класс, мы можем использовать следующий код:
```python
my_dog = Dog("Fido", "Golden Retriever")
print(my_dog.name)
print(my_dog.breed)
```
**Пример 8. Инкапсуляция**

На странице [44] приведен пример использования инкапсуляции классов:
```python
class BankAccount:
    def __init__(self, balance):
        self.__balance = balance

    def get_balance(self):
        return self.__balance

my_account = BankAccount(1000)
print(my_account

**Практические советы**

В этом разделе мы рассмотрим практические советы по написанию функций, определению функции до ее вызова в Python, а также предоставим упражнения для практического применения функций.

**Рекомендации по написанию осмысленных имён функций**

При написании функций важно давать им осмысленные имена. Это позволяет другим программистам понять назначение функции и ее поведение. Например, вместо использования имени `func1`, лучше использовать имя `count_food`. Это не только делает код более читаемым, но также помогает избежать путаницы при работе с большим количеством функций.

**Правила определения функции до её вызова в Python**

В языке Python определение функции должно предшествовать ее вызовам. Если вызов функции предшествует ее определению, то возникает ошибка `NameError`. Это связано с тем, что интерпретатор читает код строка за строкой и не знает о том, что находится ниже по течению.

**Упражнения для практического применения функций**

1. **Класс Vector3D**: Определите класс `Vector3D` с методами сложения векторов оператором `+`, вычитания векторов оператором `-`, скалярного произведения оператором `*`, умножения на скаляр оператором `*`, а также векторного произведения оператором `@`.
2. **Класс «Прямоугольный треугольник»**: Определите класс с двумя действительными числами – сторонами треугольника и методами увеличения/уменьшения размера стороны на заданное количество процентов, вычисления радиуса описанной окружности, периметра, а также определения значений углов.
3. **Класс «Одномерный масив» TArray**: Определите класс с методами конструктора без параметров, конструктора с параметрами, конструктора копирования, ввода и вывода данных, поиска максимального и минимального элементов, сортировки массива, а также поиска суммы элементов.
4. **Класс «Автобус»**: Определите класс с методами посадки и высадки одного или нескольких пассажиров, увеличения и уменьшения скорости на заданное значение, а также операций `in`, `+=` и `-=` для посадки и высадки пассажира(ов) с заданной фамилией.
5. **Класс «Снежинки

**Заключение**

В этом разделе мы суммируем все пройденные материалы и обсуждаем важность функций в программировании.

Функции представляют собой обособленный участок кода, который можно вызывать по имени. Они могут принимать данные из вызывающей программы и возвращать результат своей работы. Функции можно сравнить с небольшими программками, которые сами по себе не исполняются, а встраиваются в обычную программу.

В языке Python функции определяются с помощью оператора def. Рассмотрим пример определения функции:
```python
def count_food():
    a = int(input())
    b = int(input())
    print("Всего", a+b, "шт.")
```
Эта функция принимает два числа от пользователя и возвращает сумму этих чисел.

Функции могут не только возвращать данные, но также принимать их. Это реализуется с помощью так называемых параметров, которые указываются в скобках в заголовке функции. Количество параметров может быть любым.

Существуют два типа данных: неизменяемые и изменяемые. Изменяемые типы данных передаются по ссылке, а не копируются. Это означает, что изменения таких данных через локальную переменную обнаруживаются при обращении к ним через глобальную.

В языке Python существует множество встроенных функций, доступных сразу. Однако за время существования языка на нем было написано столько функций и классов, которые оказались востребованными множеством программистов и в разных областях, что включить весь этот объем кода в сам язык если возможно, то нецелесообразно.

Чтобы разрешить эту проблему, в программировании стала общепринятой практи

---

**Дата составления:** 25.02.2026
